[package]
name = "solver-watchdog"
version = "0.1.0"
edition = "2021"
description = "RSS/memory watchdog for hybrid Turnstile solver workers"

[[bin]]
name = "solver-watchdog"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
